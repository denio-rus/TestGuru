<tr> 
  <td><%= question.body %></td>
  <td><%= link_to t('link_show'), admin_question_path(question) %></td>
  <td><%= link_to t('link_edit'), edit_admin_question_path(question) %></td>
  <td>
    <%= link_to t('link_delete'), 
               admin_question_path(question), 
                method: :delete,
                data: { confirm: t(:sure?) } %>
  </td>
</tr>