<tr> 
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td class="text-center"><%= test.questions.count %></td>
  <td>
    <div class="btn-group" role="group" arial-label="actions">
      <%= link_to t('link_show'), admin_test_path(test), class: "btn btn-outline-primary" %>
      <%= link_to t('link_edit'), edit_admin_test_path(test), class: "btn btn-outline-secondary" %>
      <%= link_to t('link_delete'),
                    admin_test_path(test),
                    class: "btn btn-outline-danger",
                    method: :delete,
                    data:   { confirm: t(:sure?) } %>
    </div>
  </td>
</tr>